edx-ora2 Git Hooks
==================

Git hooks for the ``edx-ora2`` project:

Installation
------------

Configure the repo to use hooks from the ``/hooks`` directory by running:

.. code:: bash

    make install-git-hooks
